apiVersion: apps/v1
kind: Deployment
metadata:
  name: integrated-dashboard
  labels:
    app: integrated-dashboard
  namespace: team2
spec:
  replicas: 2
  selector:
    matchLabels:
      app: integrated-dashboard
  template:
    metadata:
      labels:
        app: integrated-dashboard
    spec:
      containers:
      - name: dashboard
        image: 10.2.2.40:5000/library/integrated-dashboard:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: DATABASE_URL
          value: "mysql://root:admin123@10.2.3.254:3306/integrated_dashboard" # From diagram: DB Access VIP
        - name: JWT_SECRET
          value: "integrated-dashboard-secret-8877"
        - name: VITE_APP_ID
          value: "YOUR_GOOGLE_CLIENT_ID_PLACEHOLDER" # 서버에서 배포 시 실제 값으로 변경하세요
        - name: GOOGLE_CLIENT_SECRET
          value: "GOCSPX-xxxxxxxxxxxxxxxxxxxxxxxxxxxx" # !!! [중요] 여기에 실제 구글 클라이언트 시크릿을 입력해주세요 !!!
        resources:
          limits:
            cpu: "1"
            memory: "1Gi"
          requests:
            cpu: "500m"
            memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: integrated-dashboard-service
  namespace: team2
spec:
  selector:
    app: integrated-dashboard
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP
